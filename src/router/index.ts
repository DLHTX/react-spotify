const ROOT = '/'

const HOME = '/home'
const HOME_DISCOVERY = `${HOME}/discovery`

const LOGIN = '/LOGIN'


const DEFAULT_ROUTE: string = HOME

const ROUTES = {
    ROOT,
    HOME,
    HOME_DISCOVERY,
    LOGIN,
    DEFAULT_ROUTE
}

export default ROUTES
